INSTALATION
	For instalation consider adding "deb http://kivfs.qwsome.eu/ debian main" into /etc/apt/sources.list
	
	After apt-get update kivfs-* packages should be available.

COMPILING
	kivfs library have to be installed in standard location /usr/lib or /usr/local/lib
	Then just run make dependents && make in root of this project.
	
RUNNING
	./bin/kivfs <mount path> -s -d
